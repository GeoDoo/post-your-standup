.logs {
  margin: 0 -24px 80px -24px;

  .log {
    margin: 24px;
    padding: 24px;
    border: 4px solid #cc0000;
    border-radius: 8px;
    white-space: break-spaces;
    word-break: break-all;
    text-align: left;
  }

  .label {
    font-weight: bold;
    font-style: italic;
  }
}
